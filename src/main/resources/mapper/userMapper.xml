<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sast.evento.mapper.UserMapper">
    <insert id="ignoreInsertUser">
        insert ignore into user (id, student_id, open_id ,email) values (#{id},#{studentId},#{openId},#{email})
    </insert>

</mapper>