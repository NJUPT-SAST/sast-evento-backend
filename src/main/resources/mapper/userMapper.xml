<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sast.evento.mapper.UserMapper">
    <update id="bindStudentId">
        UPDATE user SET student_id = #{studentId} WHERE id = #{userId}
    </update>

</mapper>